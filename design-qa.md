# Bloom Club — design QA

Дата: 16 июля 2026

## Проверенные состояния

- Экран входа, форма раскрыта, viewport 390 × 844.
- Главная, активная клубная карта, viewport 390 × 844.
- Эталон: `docs/design/bloom-login-home-selected.png`.
- Скриншоты реализации: `docs/design/qa/login-form.png` и `docs/design/qa/home-final.png`.

## Результат сравнения

### P0

Нет блокирующих дефектов.

### P1

Нет серьёзных визуальных или функциональных расхождений.

### P2 — исправлено

- Клубная карта приведена к двухколоночной композиции эталона: статус, дата, номер участницы и название клуба.
- Категории получили реальные контурные иконки и одинаковые светлые карточки.
- Уточнены сливовый цвет, радиусы, плотность отступов и типографическая иерархия.
- На главной убран дублирующий плавающий статус подписки; статус теперь встроен в клубную карту.
- Экран входа адаптирован под мобильную высоту без обрезания юридического текста.

### Допустимые адаптации

- Эталон показывает более широкий концептуальный кадр, поэтому на телефоне элементы собраны в вертикальный поток.
- Блок розыгрыша использует реальные данные приложения; в локальном preview показано штатное пустое состояние.
- Навигационные названия и существующая бизнес-логика приложения сохранены.
- Вход по Telegram и VK остаётся раздельным. Автоопределение источника и кнопка получения нового кода не добавлялись.

## Проверки

- `npm run build`: успешно.
- TypeScript: без ошибок.
- Vite production build: успешно.
- Экран входа: отдельные поля Telegram, VK и реферального кода сохранены.
- Главная: нет горизонтального переполнения на 390 px; нижняя навигация остаётся доступной.

**final result: passed**
